Original prompt: OpenTTD-like isometric persistent webgame graphics demo with realistic city, OpenGFX2 assets, no visible UI.

2026-06-01T09:30:00.000Z - Economy audit store — durable append-only event log: the unbounded in-memory `TradeLedger` now drains to a durable, append-only audit log on the snapshot-persistence cadence, and the live ledger is bounded as a side effect. New `EconomyEventStore` trait (`sim-core/persistence.rs`) with `InMemoryEconomyEventStore` + `PostgresEconomyEventStore` (`sim-server`); migration `202605310001_economy_events.sql` creates `economy_events(id BIGSERIAL PK, world_id, tick, event_type, payload JSONB, created_at)` indexed `(world_id,id)` + `(world_id,tick)`, and `append` does one batched `UNNEST` multi-row insert (payloads ride as `text[]`, cast to `jsonb` server-side — the most portable sqlx array bind). `EconomyEvent::event_type()` tags each of the 13 variants. The flush lives in the sim-server persist loop (NOT an ECS system — the DB append is async): the `CollectPersistData` mutation now also carries the unflushed ledger tail via `pending_ledger_audit` (non-mutating, `ledger.0[cursor..]`); a new persist Phase 2d appends best-effort and, on success, sends a fire-and-forget `CommitLedgerAudit { count }` mutation that advances `LedgerAuditCursor` and trims the live ledger to the last `PERSISTED_LEDGER_TAIL` (1024) — the trim is clamped to the cursor so events the economy appended mid-flush stay pending for the next cycle. A failed append never advances the cursor, never trims, and never halts the sim (best-effort; retried next cycle — distinct from the *authoritative* world-event store whose append failure rejects the command). On hydrate, `init_ledger_audit_cursor` sets the cursor to the restored `ledger_tail` length (after economy restore + demo seed, with `EconomyPlugin` having installed the cursor at its default 0) so the already-appended tail is not re-appended; the trade-off (acceptable for an audit log) is that a crash between the last flush and shutdown loses at most the unflushed window. Observability only — the `EconomySnapshotStore` remains the recovery source of truth, and query/read APIs are deferred to a later slice (the table is indexed for them). Backend-only: no wire/frontend change, and goods conservation + economy determinism are untouched (the append is a read-only side effect of already-emitted events). Verified: `cargo fmt --check` + clippy `--workspace --all-targets -D warnings` + `cargo test --workspace --all-targets` green — sim-core 372 (incl. 5 new `economy::tests::audit`: event-type stability, flush-appends-then-trims-and-bounds, init-skips-restored-tail, pending-non-mutating, in-memory append order) and sim-server 88 (incl. 2 new persist-loop tests: append-then-commit-prevents-reappend, and best-effort-failure-retries); the opt-in `PostgresEconomyEventStore` integration test (`ABUTOWN_TEST_DATABASE_URL`) passed against the live database, exercising the migration + batched UNNEST insert + round-trip. Spec `docs/superpowers/specs/2026-05-31-economy-audit-store-design.md`.

2026-05-31T10:27:56.000Z - Visible traders made LOD-consistent (PR #66): the economy render bridge now materializes a trader-agent only while its current position is in an observed (Active/Hot) chunk and despawns it when the trader walks out — ghost-free via a one-tick dirty-then-despawn (the leaving tick emits `left_agents` for the chunk it left, then despawn next tick), mirroring how mobility demotes its agents. `MaterializedTrader { entity, observed }` tracks the one-tick state. Render/delta cost is now proportional to observed traders, not total. Replaces the earlier keep-alive that kept unobserved trader-agents alive (a two-tier-LOD inconsistency). Verified: new `materialize_despawns_when_trader_leaves_observed_chunks` unit test + existing materialize/e2e-walk/conservation tests green; `cargo fmt --check` + clippy `-D warnings` + `cargo test --workspace --all-targets` (16 suites) green; CI (Rust + Frontend + browser smoke) green on first run.

2026-05-31T09:54:04.000Z - Visible economy traders (PR #64): economy traders now render as real walking mobility agents on the HPA*/flow-field footway graph between their source and destination markets, fed through the per-tick mobility delta (client interpolates) with a distinct trader sprite (via a `trader:` `sprite_key` prefix — zero protobuf change). New render-only `TraderAgent` (no `AgentMarker`); economy `materialize.rs` bridge (`EconomySet::Materialize`) drives `Position` along the route at the trader's authoritative travel-countdown progress and inserts the entity into `DirtyAgents` each tick; `extract_from_world` excludes `TraderAgent` from mobility persistence (it is a projection of the persisted economy); `economy::seed::seed_demo_economy` idempotently bootstraps a two-market + one-trader demo on BOTH the fresh and hydrate runtime paths (the production server always hydrates). `scripts/smoke-visible-traders.mjs` browser smoke proves the trader appears and moves. Economy core stays graph-free/countdown-authoritative/conservation-exact. Verified: clippy `-D warnings` + `cargo test --workspace` (sim-core 362 / sim-server 85), frontend 218 vitest + typecheck, and the mandatory browser smoke green; CI green.

2026-05-29T15:34:40.000Z - Historical plan cleanup: all `docs/superpowers/plans/*.md` checklists are now closed so stale `- [ ]` entries no longer look like active work. Older plans carry an archive status that points readers to `progress.md` and later plans as the current source of truth; the superseded backend-authoritative transit plan explicitly notes that Traffic Simulation Hardening removed runtime tram movement again.

2026-05-29T15:30:46.000Z - Traffic simulation hardening merged and docs reconciled: runtime transit/tram movement is retired in favor of backend-authoritative road traffic. `TrafficRoutes` now owns road-only vehicle routes, mobility vehicle state and persistence resolve cars through those routes, `VehicleKind`/`VehicleKindDto` are car-only at runtime boundaries, and old tram DTO/render/diagnostic success paths are gone. Frontend diagnostics now expose `traffic { routes, cars, movingCars, stuckCars, invalidRouteCars }`; render smoke asserts car-only backend vehicles and absence of `mobilityTrams`/`train` runtime diagnostics. Cleanup after merge marked `docs/superpowers/plans/2026-05-29-traffic-simulation-hardening.md` complete and removed the orphaned train render style helper/test that no production code imported.

2026-05-28T16:02:14.000Z - Backend-authoritative transit pass: canonical `spawns.tram_lines` now seeds deterministic backend `VehicleKind::Tram` records from Base World rail paths, and runtime startup/hydration installs those authored transit lines into the routing graph before mobility hydration. Old local train motion is removed from production frontend code (`trainMotion` deleted, no `buildNorthboundTrainPath` / `trainWrappedOffset` / local train loop remains); canvas train visuals now project backend tram vehicles from the mobility stream. Viewport subscription interest now includes known tram chunks plus a one-chunk ring so backend LOD tracks transit vehicles that the UI is following, tram vehicles tick server-side even without a viewport subscriber, and the mobility reducer ignores stale chunk snapshots/deltas by tick to avoid older subscription snapshots deleting newer backend state. Hydration rejects stale persisted tram snapshots that have the right count but missing/stacked declared tram identities, forcing a fresh Base World seed instead of preserving old bad mobility state. E2E now asserts 4 backend trams, no retired raster asset requests, backend source diagnostics, and actual backend-driven movement for agents, cars, and trams. Verification: `npm test` passed 41 files / 192 tests; `npm run build` is clean; `cargo fmt --manifest-path backend/Cargo.toml --all` passed; `cargo test --manifest-path backend/Cargo.toml -p sim-core` passed 220 lib + 3 base-world + 2 LOD lifecycle + 8 persistence tests; `cargo test --manifest-path backend/Cargo.toml -p sim-server` passed 50 lib + 15 HTTP + 10 WebSocket tests; `cargo clippy --manifest-path backend/Cargo.toml --workspace --all-targets -- -D warnings` is clean; `PATH=/Users/ramonfuglister/.rustup/toolchains/stable-aarch64-apple-darwin/bin:$PATH npm run test:e2e -- tests/e2e/render-smoke.spec.ts` passed.

2026-05-28T05:05:58.000Z - Base World Cutover Task 8 complete: persisted chunk and mobility snapshots are now gated by explicit `SnapshotCompatibility { base_world_id, base_world_schema_version }` on every store read/write. Postgres migration `202605280001_snapshot_base_world_metadata.sql` adds indexed base-world metadata columns to `chunk_snapshots` and `mobility_snapshots`; legacy rows with null metadata are ignored instead of being upgraded silently. In-memory stores use the same compatibility tuple as part of their key. Hydration no longer discards compatible vehicleless snapshots through the old expected-car heuristic, and production mobility seeding no longer creates fixed tiny tram vehicles or exposes `legacy_seeded_*`, `initial_world`, `tiny_world`, `trams_total`, or `CityNetwork::empty_for_world` paths. Fresh base-world startup now seeds 1011 agents and 51 car vehicles; visual trains remain frontend/base-world rail rendering rather than hardcoded backend tram seed data. Acceptance grep is clean across `backend/crates/sim-core/src backend/crates/sim-server/src backend/crates/sim-core/tests backend/crates/sim-server/tests src tests` for `fallback|legacy_seeded|trams_total|empty_for_world|tiny_world\\(|initial_world\\(|mobility_snapshot_missing_expected_cars|seeded_mobility_snapshot`. Verification: `cargo fmt --manifest-path backend/Cargo.toml --all` passed; targeted RED/GREEN guards were exercised for snapshot compatibility; `cargo test --manifest-path backend/Cargo.toml -p sim-core` passed 217 lib + 3 base-world + 2 LOD lifecycle + 8 mobility persistence tests; `cargo test --manifest-path backend/Cargo.toml -p sim-server` passed 48 lib + 15 HTTP + 10 WebSocket tests; `cargo clippy --manifest-path backend/Cargo.toml --workspace --all-targets -- -D warnings` is clean; `npm test` passed 41 files / 192 tests; `npm run build` is clean; `PATH=/Users/ramonfuglister/.rustup/toolchains/stable-aarch64-apple-darwin/bin:$PATH npm run test:e2e -- tests/e2e/render-smoke.spec.ts` passed. Dev stack was restarted after E2E and is reachable at `http://127.0.0.1:5175/`.

2026-05-27T00:27:19.000Z - Phase 8e verification pass: graph flow fields and live walking route execution are implemented. `FlowFieldRouter` builds profile-aware reverse-Dijkstra fields, corridor-scoped fields use HPA cluster corridors without global retries, `FlowFieldCache` is installed/cleared with runtime routing resources, walking agents materialize and advance `ActiveRoute` multi-edge graph paths, and persisted `active_route` data now validates canonical keys, cursor bounds, step connectivity through shared polyline endpoints, profile transitions including WalkTransit stop-only board/alight rules, and finite lengths on hydration. Persisted raw graph edge ids and destination node ids are normalized after snapshot graph rebuild because the stable authority in JSONB is the canonical edge key and final resolved edge; malformed canonical keys still fail explicitly. Frontend proto decoding now rejects missing agent state, missing world coordinates, missing chunk coordinates, invalid walking states, and unspecified/unknown directions instead of rendering valid-looking defaults; graph-native walking ids use canonical `edge:<id>` keys. Acceptance grep for fallback language/default coordinates is clean across `backend/crates/sim-core/src backend/crates/sim-server/src src tests`; the implementation grep confirms `FlowFieldCache`, `ActiveRoute`, `route_assignment_system`, and `route_advance_system` are present in sim-core. Verification: `cargo test -p sim-core routing::flow_field -- --nocapture` passed 11 tests; `cargo test -p sim-core mobility:: -- --nocapture` passed 85 tests; `cargo test -p sim-core --test mobility_persistence_round_trip -- --nocapture` passed 8 tests; `cargo test -p sim-server runtime_ -- --nocapture` passed the runtime-filtered set; full `cargo test --workspace -- --nocapture` passed 310 non-doc tests plus doc-tests; `cargo clippy --workspace --all-targets -- -D warnings` is clean. Frontend verification: `tsc --noEmit --pretty false` passed, targeted `mobilityProtocol` Vitest passed 23 tests, and full Vitest passed 32 files / 177 tests. Browser smoke caveat: `node scripts/smoke-7b.mjs` reached the app, exchanged binary-only frames, got 30 mobility chunk snapshots, sent chunk subscribe/unsubscribe frames, panned successfully, and had no console errors, but failed `got_chunk_deltas_per_tick` and `client_receives_entity_data` on both the local Postgres stack and a fresh in-memory `build_app()` stack because `/mobility` served an empty 19-byte protobuf in this local start state. Perf caveat: targeted `tick_100k_all_active` bench was attempted; an initial run was ~13.27 ms, then later runs under heavy host `fileproviderd`/`cloudd` CPU load reported ~44.8 ms, so the local benchmark evidence is noisy and above the 8d +5% budget. A hot-path guard was added so route assignment returns immediately when HPA/FlowField resources are absent in seed/bench worlds instead of scanning 100k walkers just to count skipped assignments.

2026-05-26T16:24:26.000Z - Phase 8d verification pass: corridor-HPA* routing is implemented on top of the Phase 8c graph and A* layer. `HpaIndex` builds deterministic fixed-size clusters, detects real cross-cluster portals, records profile-aware abstract adjacency, and `HpaRouter` routes through explicit cluster corridors before delegating final legality to constrained 8c A*. `HierarchicalRoutingPlugin` is installed after `PathfindingPlugin`; runtime seeded Zurich tests verify the HPA resource and a seeded tram route, including refresh after mobility hydration or test mobility replacement changes routing resources. Review fixes added explicit HPA `MissingNode` validation before cluster resolution and covered the `set_mobility_for_test` stale-index path. Targeted routing tests pass (`pathfinding` 10, `hpa` 10, `plugin` 4), targeted `sim-server runtime_` tests pass (17 filtered runtime/app tests) plus the focused HPA refresh helper test, workspace cargo tests pass (267 tests total plus doc-tests), and clippy `-D warnings` is clean. Frontend verification is clean: `tsc --noEmit` passes and Vitest passes 166 tests across 32 files. Browser smoke `scripts/smoke-7b.mjs` is 9/9 green against a fresh in-memory backend (`build_app()` path) with 279 received binary frames, 30 mobility chunk snapshots, 184 mobility chunk deltas, 0 text frames, and no console errors; the normal Postgres-backed dev stack still hydrated quiet mobility data and produced snapshots but no deltas, matching the known persisted-state caveat. Perf bench `tick_100k_all_active` is inside the Phase 8c +5% budget of 11.599 ms: mean 11.393 ms [11.303, 11.499], median 11.234 ms [11.198, 11.280]. Wire protocol, frontend state, and `mobility_snapshots` JSONB schema remain unchanged.

2026-05-25T18:03:19.000Z - Phase 8c Task 7 verification pass: A* routing, mode-aware profiles, point-to-node requests, `PathCache`, and `PathfindingPlugin` are implemented on top of the Phase 8b graph. Targeted `sim-core routing::` tests pass (41 routing tests), targeted `sim-server runtime_` tests pass (15 filtered runtime/app tests), full backend workspace tests pass (250 tests total plus doc-tests), and `cargo clippy --workspace --all-targets -- -D warnings` is clean. Frontend verification is clean: `tsc --noEmit` passes and Vitest passes 166 tests across 32 files. Browser smoke `scripts/smoke-7b.mjs` is 9/9 green against a fresh in-memory backend (`build_app()` path) with 458 received binary frames, 30 mobility chunk snapshots, 365 mobility chunk deltas, 0 text frames, and no console errors; the normal Postgres-backed dev stack still hydrates stale/quiet mobility data and produced snapshots but no deltas, matching the known Phase 8b verification caveat. Perf bench `tick_100k_all_active` is inside budget: mean 11.173 ms [11.079, 11.281], median 11.047 ms [10.950, 11.181], below the Phase 8b +5% budget of 12.362 ms. Wire protocol and `mobility_snapshots` JSONB schema remain unchanged.

2026-05-24T07:45:36.000Z - Phase 8b Task 13 verification pass: legacy routing fallbacks are cut from the Rust mobility core. Acceptance greps are clean for deleted `Routes`/`Stops`/`LinkPolylines`, `RouteRecord`/`StopRecord`, and `RouteId`/`LinkId`/`StopId`; routing module listing is clean at the expected 8 files; `runtime_has_populated_routing_graph` verifies the seeded Zurich runtime has 418 graph nodes, 209 graph edges, 3 transit lines, and a `NodeSpatialIndex` sized to the graph. Full backend workspace tests pass (230 cargo tests total), `cargo clippy --workspace --all-targets -- -D warnings` is clean, `tsc --noEmit` is clean, full Vitest is clean (166 tests across 32 files), and the browser smoke `scripts/smoke-7b.mjs` is 9/9 green against a fresh in-memory runtime (486 received binary frames, 30 mobility chunk snapshots, 393 mobility chunk deltas, 0 text frames, no console errors). Perf bench `tick_100k_all_active` is back inside budget after removing a per-walker `graph.edge_by_legacy()` lookup from the advance hot path: median 11.773 ms vs Phase 8a baseline 11.84 ms (delta -0.6%; mean 12.216 ms, within the <=12.43 ms +5% budget). Test-harness cleanup: removed stale untracked `routing/graph 2.rs`, narrowed the retired-asset grep test so it no longer scans the imported Pak128 asset tree, and mocked Supabase in the card-hand URL unit test so Vitest does not load the real package for a pure URL helper assertion. Postgres-backed `cargo run -p sim-server` can hydrate a stale high-tick mobility snapshot, so fresh-state smoke verification used the same `build_app()` in-memory runtime path as integration tests rather than mutating the local database.

2026-05-14T08:04:30.446Z - Visual QA: replaced hand-drawn road strokes with OpenGFX2 road sprites, moved central river to a small edge creek, reduced road-grid density, increased building scale, and kept verification on port 5175 only.
2026-05-14 - Zurich river city world: imported broad OpenGFX coverage, added deterministic 256x256 flat river-city layout, integrated validated roads/rails/buildings/trees into the existing Canvas demo, and captured visual QA at artifacts/abutown-zurich-river-city-2026-05-14.png.
2026-05-14T14:11:40.000Z - Camera UX: replaced map wrapping with a bounded fixed-map camera, added damped pan/zoom targets, added outskirts/edge-exit/mist rendering, and restored Vite preview support for Playwright smoke tests.
2026-05-14T15:56:52.000Z - Screenshot QA pass: narrowed the flat Limmat, added connected road bridge spans, restricted buildings to finished OpenGFX first-row frames, reduced blue construction-like high-rises, and captured artifacts/abutown-zurich-river-city-2026-05-14-v3.png.
2026-05-14T16:22:28.000Z - Organic city-planning pass: tightened residential density falloff, opened the river corridor, removed non-bridge riverbank road stubs, reduced adjacent grid runs to 2, clustered forests with irregular sparse pockets, and captured artifacts/abutown-zurich-river-city-2026-05-14-v5.png.
2026-05-14T22:17:00.000Z - Train task: add one visible northbound OpenGFX train on the single vertical rail corridor; it should fade in from the south edge, travel upward, and fade out at the north edge.
2026-05-14T23:31:00.000Z - Train QA fix: moved the single rail corridor to visible x=150, enlarged and tightened the OpenGFX train consist, verified screenshots at mid-map, and kept only the 5174 dev server running.
2026-05-15T13:40:48.000Z - Backend cleanup: verified the Rust backend workspace, corrected backend README scope, and removed stale tracked duplicate ` 2.ts` test files.
2026-05-15T15:56:00.000Z - Mobility client bridge: added opt-in frontend mobility protocol/state/client modules, diagnostic canvas overlay markers, Vite dev proxy support, and offline-safe smoke coverage.
2026-05-15T20:55:00.000Z - Chunk recovery: switched snapshots to full-state sparse encoding, added chunk-version-aware event replay via Chunk::from_snapshot and Chunk::apply_event, command_id idempotency via unique constraint on world_events with race-handler, smarter snapshot trigger (dirty + 30s ceiling), and runtime hydration on startup via hydrate_from_stores.
2026-05-16T07:15:00.000Z - Mobility population: deterministic initial seeder for routes/stops/vehicles/agents, Postgres mobility_snapshots table with UPSERT semantics, full-state mobility persistence on the existing snapshot loop, and mobility hydration at server startup via the extended hydrate_from_stores signature.
2026-05-16T08:50:00.000Z - Visible backend mobility: added RoadVehicleWorld + Postgres road_vehicle_snapshots, extended Agent/Vehicle/RoadVehicle DTOs with world_coord + direction + sprite_key (server-computed each tick via new mobility_geometry module), replaced frontend buildPedestrians/buildCars with backendMobilityDrawables projector, switched selection/inspector to backend ids. Canvas now renders the 20 seeded pedestrians + 80 seeded road vehicles + 4 transit vehicles directly from server-authoritative state. First phase of the million-agent roadmap.
2026-05-16T10:00:00.000Z - Mobility frame interpolation: backend tick raised from 1 Hz to 10 Hz, WorldSummaryDto exposes tick_period_ms, frontend mobility/road-vehicle state now buffers prev+current+lastTickAt per entity, drawables projector linearly interpolates world_coord by t = (now - lastTickAt) / tick_period_ms. Canvas renders smooth 60 fps motion between server ticks. Phase 2 of the million-agent roadmap.
2026-05-16T12:03:23.000Z - Procedural population & shared path network: generated `data/city/zurich-network.json` via `scripts/generate-city-network.mjs` (3 arterials + 160 pedestrian corridors). Backend `sim_core::city_network` loads it at startup; `mobility::seed::from_network` scales `MobilityWorld` to ~1015 visible entities — ~960 walking agents + 51 car-Vehicles each driven by an InVehicle driver-Agent + 4 trams. `LinkGeometry` is now a polyline (`world_coord_at_progress` walks arc length). `VehicleKind { Car, Tram }` discriminates the sprite class on both protocol and storage. Retired `RoadVehicleWorld` end-to-end: deleted `road_vehicles.rs`, `postgres_road_vehicles.rs`, `RoadVehicleId`, the snapshot store, both `RoadVehicleDto` types, the `ServerMessageDto::RoadVehicleDelta` variant, the `/road-vehicles` HTTP endpoint, `roadVehicleProtocol.ts`, `roadVehicleState.ts`, and the parallel frontend client/state paths. Cars now come from `MobilityWorld.vehicles.filter(kind=Car)`; in-vehicle agents are filtered out of delta broadcasts (kept in snapshot). New migration `202605160005_drop_road_vehicle_snapshots.sql` cleans up the orphan Postgres table. One-time pre-deploy: operator should `DELETE FROM mobility_snapshots` because pre-Phase-3 rows lack the `kind` field in the JSONB payload and will fail hydration. Phase 3 of the million-agent roadmap.
2026-05-16T15:17:21.000Z - Viewport-filtered mobility replication: introduced `ClientMessageDto::ChunkSubscribe`/`ChunkUnsubscribe` plus per-connection `ConnectionState` in the WS task (`stream_world_deltas` rewritten as `tokio::select!` loop). `MobilityDelta` is now filtered per connection through `build_filtered_mobility_delta_dto` which produces `changed_*`/`left_*` against the per-connection subscription and `last_visible_ids` sets. `chunk_of(x, y, chunk_size)` projects world coords onto the chunk grid via `div_euclid` (correct for negative coords). On subscribe/unsubscribe the server emits a synthetic delta so clients learn join/leave immediately; per-tick empty deltas are dropped. Frontend `chunkSubscriptionClient.ts` sends one full-world `chunk_subscribe` (8×8 grid for 256×256 world) right after WS open; `applyMobilityDelta` drops ids listed in `left_agents`/`left_vehicles` before applying changes. Two-client integration test in `websocket.rs` verifies disjoint entity sets across disjoint subscriptions. Phase 4 of the million-agent roadmap — backend can now sustain ~10k entities with per-client viewport-scoped bandwidth.
2026-05-21T04:37:00.000Z - Phase 8a — World Unification Foundation: dissolved the dual-world topology (`MobilityWorld` wrapper + `ChunkRegistry::HashMap`) into a single `bevy_ecs::World` held directly on `SimulationRuntime`. Chunks are now entities carrying `ChunkCoordComp`, `ChunkSize`, `Tiles(Vec<TileRecord>)` dense terrain, `ChunkVersion`, `DirtyTiles`, `LastPersistedVersion`, `LastSnapshotAt`, `ChunkSubscriberCount`, `LodCooldown`, and exactly one of the mutually-exclusive zero-sized markers `AsleepChunk`/`WarmChunk`/`ActiveChunk`/`HotChunk` — archetype-based LOD queries (`Query<&Tiles, With<HotChunk>>`) replace the per-call enum branches. Tile-entity scaffold ships ready: `Tile` marker, `LocalIndex`, `BelongsToChunk` Bevy 0.18 first-class Relationship with auto-maintained `ChunkTiles` reverse, plus a `spawn_functional_tile()` helper. No domain components shipped (Home/Workplace/Storage are future phases). Plugin composition: a local `SimPlugin` trait (no `bevy_app` dep) installs subsystems against the shared World+Schedule. `CorePlugin` registers all foundation resources (`ChunksByCoord`, `TickClock`, `EventCount`, `ChunkSizeRes`, `WorldDimensions`, `DirtyChunks`, `WorldIdRes`, `DeterministicRng`, `SnapshotProviders`, `MigrationRegistry`), the four buffered foundation messages (Bevy 0.18 `#[derive(Message)]` — `ChunkLoaded`, `ChunkUnloaded`, `TileChanged`, `ChunkLodChanged`), the `CoreSet` ScheduleLabel chain (`ChunkLifecycle → TileMutation → LodReclassify → EventEmit`), and the chunk-LOD reclassify system. `MobilityPlugin` and `PersistencePlugin` are siblings — they register their own resources/messages/systems against the same World. Two `SnapshotProvider` implementations (`ChunkSnapshotProvider`, `MobilitySnapshotProvider`) are registered in the `SnapshotProviders` resource by `PersistencePlugin`; the Postgres schema (`chunk_snapshots` + `mobility_snapshots` JSONB tables) is byte-identical to Phase 7c. Wire protocol bytes are byte-identical (no proto changes). `DeterministicRng` resource is seeded from `blake3(world_id)` and is the only RNG source (CI grep blocks `thread_rng`/`rand::random` in sim-core/sim-server). `SnapshotProvider` trait + `MigrationRegistry` resource are installed empty; future plugins register schema migrations there without touching the persist loop. The tile mutation path now flows through `apply_set_tile_kind_ecs` on the shared World: bumps `ChunkVersion`, populates `DirtyTiles`, marks `DirtyChunks`, writes `TileChanged` messages — wire output unchanged. The chunk LOD reclassify system runs on the entity side in `CoreSet::LodReclassify`; a compat shim continues to populate the legacy `ChunkActivities` resource so mobility's restored `promote_warm_to_active_system` and `demote_active_to_warm_system` (preserved for their agent-spawn/despawn side-effects) can drive flow-cell agent lifecycle unchanged. All 204 cargo workspace tests pass; clippy `-D warnings` clean; tsc clean; vitest 166 pass; smoke `scripts/smoke-7b.mjs` 9/9 with binary frames (127 mobility chunk deltas across 7 chunks observed in-session). Perf bench `tick_100k_all_active` 11.84 ms vs Phase 7c baseline 13.18 ms (delta -10.2% — Phase 8a is faster, well under the +5% regression budget). Acceptance greps confirm zero `ChunkRegistry`, zero `pub struct MobilityWorld`, zero `thread_rng`/`rand::random` matches across `backend/crates/sim-core` and `backend/crates/sim-server`, and zero `mod chunk_registry` / `use crate::chunk_registry` references. Spec `docs/superpowers/specs/2026-05-20-world-unification-foundation-design.md`, plan `docs/superpowers/plans/2026-05-20-world-unification-foundation.md`. Commits `5f609fe` (T1) / `93d90f2` + `cb22315` + `83f25bf` (T2) / `bb8b404` (T3) / `d8f4113` (T4) / `2cd9077` (T5) / `2704b42` (T6) / `227ccf4` (T7) / `d4046eb` + `f468032` (T8) / `fcf669c` (T9) / `22a0af8` (T10) / `e5a88f6` (T11) plus this entry's commit (T12). Phase 8a closes — Foundation ready for the plugin-composition phases that follow (8b graph + spatial index, 8c A* + multi-modal routing, 8e flow fields, 8g domain tiles, 8h economy + ledger).
2026-05-20T16:38:52.000Z - Binary wire protocol via Protobuf: replaced JSON wire encoding (WS + HTTP) with Protocol Buffers (proto3). Single source of truth at `backend/crates/protocol/proto/abutown.proto` defines 28 messages and 4 enums covering hello / pulse / mobility-chunk delta-and-snapshot / world-event on the server envelope, chunk subscribe-and-unsubscribe + client command on the client side, plus every HTTP response body. Rust codegen via `prost-build` in `backend/crates/protocol/build.rs`; TypeScript codegen via the local `@bufbuild/protoc-gen-es` plugin driven by `@bufbuild/buf` into `src/backend/proto/` (gitignored, regenerated by `npm run generate:proto` which now precedes `npm run build`). WebSocket frames are `Message::Binary` carrying `prost::Message::encode_to_vec(&wire::ServerMessage)`; frontend sets `socket.binaryType = 'arraybuffer'`, decodes via `fromBinary(ServerMessageSchema, …)`. HTTP endpoints `/health`, `/world`, `/mobility`, `/chunks/{x}/{y}`, and `POST /commands` return `Content-Type: application/x-protobuf` via a new `proto_response<M>` helper; the same handler accepts proto request bodies through a new `ProtoBody<M>` axum extractor. `RuntimeReadView`'s wire-facing fields all carry proto types; conversion helpers (`agent_dto_to_proto`, `vehicle_dto_to_proto`, `world_summary_dto_to_proto`, `world_event_dto_to_proto`, `tile_pulse_dto_to_proto`, `chunk_snapshot_dto_to_proto`, `mobility_snapshot_dto_to_proto`, `health_dto_to_proto`, `stop_dto_to_proto`, `direction_to_proto`, `tile_kind_to_proto`, `chunk_state_to_proto`) bridge legacy serde DTOs to proto wire types where the runtime still produces serde internally. DB persistence (`mobility_snapshots` and `chunk_snapshots` JSONB columns) intentionally stays JSON-encoded — wire and storage are now separate concerns. Serde `Serialize`/`Deserialize` derives stripped from 22 wire-only DTOs in `abutown-protocol/src/lib.rs`; remaining serde derives are on storage-bound types (`ChunkSnapshotDto`, `TileMutationDto`, `ChunkCoordDto`, `ChunkStateDto`, `TileKindDto`, `WorldEventDto`, `TileKindSetEventDto`) and the `WorldId` / `EntityId` newtype ID wrappers used pervasively for in-Rust comparisons. Two reverse proto→serde helpers (`client_command_proto_to_dto` + `tile_kind_proto_to_dto`) survive on the inbound HTTP `POST /commands` path because `Mutation::ApplyCommand` still carries `ClientCommandDto`; the refactor to make `Mutation::ApplyCommand` carry `wire::ClientCommand` directly was deferred as a follow-up (~7 test sites to migrate). Wire-size bench `backend/crates/protocol/benches/wire_size.rs` for a `MobilityChunkDelta` with N walking agents: 1=3.85×, 10=2.82×, 50=2.71× (10775→3981 bytes), 100=2.69× (21382→7949 bytes). The 3× gate in the spec was aspirational and the actual asymptote sits at ~2.7× because string identifiers (`agent:walker:N`, `link:walk:corridor:22`, `pedestrian:N`) dominate payloads at scale — both JSON and protobuf encode the string CONTENT identically (length-prefixed UTF-8 bytes), so protobuf's win comes from eliminating JSON syntax overhead (quotes, brackets, commas, field names) which is roughly fixed-per-record. At small payloads (n=1) protobuf wins big (3.85×); at 50+ agents it's a steady ~2.7×. The migration still pays off: schema-first single source of truth, compile-time type safety on both ends, forward-compat via protobuf field tags, lower CPU encoding cost (`encode_50_agent_delta` runs in ~2.2 µs), and a meaningful 63% reduction in wire bytes per delta at the realistic 50-agent workload. All 189 cargo workspace tests + 166 vitest + clippy `-D warnings` + tsc clean. Browser smoke `scripts/smoke-7b.mjs` 9/9 with binary frames (`framereceived.payload` decoded via `ServerMessage.fromBinary` against the proto generated in `src/backend/proto/abutown_pb.ts`, counted by `body.case` discriminator; `no_text_frames` check replaces the old `no_legacy_mobility_delta` invariant). Acceptance grep `serde_json::{to_string,from_str,from_slice}` returns zero matches in `backend/crates/sim-server/src/` excluding tests, persistence, and the out-of-scope `/cards`/`/card-hand` endpoints. Curl-debug recipe: `curl -s http://127.0.0.1:8080/world | protoc --decode=abutown.v1.WorldSummary -I backend/crates/protocol/proto abutown.proto`. Spec `docs/superpowers/specs/2026-05-20-binary-wire-protobuf-design.md`, plan `docs/superpowers/plans/2026-05-20-binary-wire-protobuf.md`. Commits `c295222` / `3630922` / `58f519c` / `fd207f8` / `0c628f9` / `d1d81d0` / `c9fdb35` / `80285d1` / `ccb3b29` / `d9a7da9` plus this entry's commit. State-of-the-art 2026 wire protocol — schema-first, type-safe, ~2.7× smaller payloads at realistic scale, forward-compat by design.
2026-05-20T08:05:54.000Z - Phase 7c — Tick-owned runtime + Arc-snapshot lock-free reads: removed `Arc<RwLock<SimulationRuntime>>` from `AppState` entirely. A dedicated `tick_loop` task now owns the `SimulationRuntime` exclusively and is the sole mutator. Writes (HTTP `/commands`, WS `chunk_subscribe` / `chunk_unsubscribe` / disconnect, snapshot-persist mark-and-collect) flow through `tokio::sync::mpsc::UnboundedSender<Mutation>`; the tick task drains the entire queue under one borrow as Phase 0 of each tick. Reads (HTTP `/health`, `/world`, `/mobility`, `/chunks/{x}/{y}`, WS `hello` frame, WS lagged-recovery snapshot, snapshot-persist collection) consume `Arc<ArcSwap<RuntimeReadView>>` wait-free — the view is published once per tick and contains pre-materialized DTOs (`world_summary`, `health`, `chunk_snapshots`, `mobility_chunk_snapshots`, `mobility_full_dto`, `chunk_subscriber_counts`, `per_chunk_deltas`, `pulse_sequence`). `Mutation::ApplyCommand`, `Mutation::SubscriptionDiff` and `Mutation::CollectPersistData` carry `oneshot::Sender` reply channels — the drain handler fills them so HTTP/WS handlers awaken with the result; `Mutation::MarkChunkSnapshotsPersisted` is fire-and-forget. Persist loop runs entirely off the view + a mutation request for the `MobilityWorld` clone (avoids the lossy DTO round-trip the spec originally proposed via `write_dto`). The migration was staged: task 2 added the view alongside the existing lock, tasks 3-4 migrated read sites, task 5 migrated write sites via the mpsc queue, task 6 dropped the RwLock and moved ownership into the tick task. Tick_loop is panic-supervised — a panic logs via `tracing::error!` and calls `std::process::abort()` so an external supervisor can recover instead of running a zombie server. Worst-case command latency is one tick interval (100 ms) — accepted trade-off for non-realtime gameplay. Bench: `tick_100k_all_active` 12.77 ms (Phase 6 baseline) → 13.18 ms (+3.2% — within the 12-17 ms session-to-session variance documented in the 2026-05-20T05:21:09 entry; the additional per-tick view-build cost is balanced against eliminated read-lock contention); `tick_100k_with_5_subscribed_chunks` 37.0 µs → 37.2 µs (unchanged within noise). All workspace cargo tests + clippy `-D warnings` + tsc + vitest 158/158 clean. Acceptance greps confirm zero `RwLock<SimulationRuntime>` / `Arc<RwLock<SimulationRuntime>` matches anywhere under `backend/crates/` and zero `runtime.read()/write()` calls in `app.rs`. Browser smoke `scripts/smoke-7b.mjs` 9/9 pass against a fresh database (re-verified post-merge after a stale-state false-negative — agents that hydrated from a pre-7c snapshot were stuck at `Walking { progress: 1.0 }` because they were `agent:lod:*` LOD-spawned entities whose first plan stage is `Activity`, which falls into `stop_arrival`'s catch-all and never transitions; a `DELETE FROM mobility_snapshots WHERE world_id='abutown-main'` before restart bypasses the issue, and a fresh seed produces 942 walking agents + 46 vehicles that tick normally and produce per-chunk deltas). The stuck-LOD-agent issue is pre-existing simulation logic, not Phase 7c. Spec `docs/superpowers/specs/2026-05-20-phase-7c-design.md`, plan `docs/superpowers/plans/2026-05-20-phase-7c.md`. Commits `64a546a` / `dfdbf32` / `87f9065` / `21f20bc` / `fc13514` / `a2e915d` / `bc05332` / `93e1cc4` / `972fb0c` / `749a071` / `d7b20be` / `c11f6bd`. Phase 7c of the WS scalability arc — closes the architectural sequence 7a (RwLock for parallel reads) → 7b (per-chunk broadcast channels) → 7c (RwLock removed, single-writer tick task + lock-free view).
2026-05-20T05:21:09.000Z - Mobility tick perf rework — reviewer followups: three non-blocking follow-ups from the final code review of the Phase 6 perf rework. (1) `boarding_alighting_system` phase B.1 was the last residual O(N_vehicles × N_stops) pattern — replaced `stops.0.values().find(...)` per vehicle with a one-shot `HashMap<(&RouteId, link_index), &StopRecord>` built at the start of the system, filtered to stops with progress=1.0; each vehicle then does a single O(1) lookup, with a progress-equals-1.0 fast-fail guard before it. (2) `AgentsByChunk` / `VehiclesByChunk` switched from `HashMap<ChunkCoord, Vec<Entity>>` to `HashMap<ChunkCoord, HashSet<Entity>>` — per-entity removal in `track_chunk_populations_system`'s incremental path was O(K) via `bucket.retain(|e| *e != entity)`; with `HashSet` the remove is O(1) and buckets are structurally unique-by-entity (`Vec::push` didn't enforce that). `despawn_vehicles_into_flow_cell` signature changed from `&[Entity]` to `&HashSet<Entity>`. (3) `compute_world_coord_system` now `debug_assert!`s that the computed `(x, y)` is finite before the equality-guarded write — if `world_coord_at_progress_slice` ever returned NaN, the `pos.x != x || pos.y != y` guard would always fire (NaN ≠ NaN), defeating the Task-7 change-detection win and re-triggering `Changed<Position>` for every entity per tick. The assert documents and enforces the invariant in debug builds without runtime cost in release. Bench on this host stayed at ~16 ms — verified that the +27% reported between the Task 7 capture (12.77 ms) and this session is pure session-to-session variance (same host swings 12-17 ms; same code from `209d97e` re-benched here gave 16.22 ms, matching post-followup). 96 sim-core lib tests + workspace tests + clippy `-D warnings` + tsc all pass. Commits `e92468e` / `26c757b` / `16cc627`. No spec or plan update; these are pure cleanup/correctness follow-ups on top of `b0ffa19`.
2026-05-20T04:48:00.000Z - Mobility tick perf rework (Phase 6 followup): the original 2026-05-17 progress note claimed LOD-filter overhead was the bottleneck — fresh profiling debunked that. The existing `tick_100k_with_5_subscribed_chunks` bench warms down to ~0 active entities because LOD demotes everything outside the 5 subscribed chunks (still ~37µs — essentially unchanged). New bench `tick_100k_all_active` subscribes the entire 32×16 chunk grid so 100k walkers + 1k cars stay in the ECS hot path; baseline was 24.4 ms / tick. Six optimizations land under that bench: (1) `AgentIdIndex` + `VehicleIdIndex` ECS resources mirror `MobilityWorld.by_agent_id` / `by_vehicle_id` so `boarding_alighting_system` does O(1) lookups instead of three nested 100k-entity scans (A.5, B.2, B.3) — also drops the 100k-entry A.0 pre-pass, replacing it with lazy per-candidate `chunk_is_simulated`; (2) new `NearStop` marker (inserted by `walk_advance_system` on progress saturation, removed by `stop_arrival_system` post-transition AFTER the chunk-simulated check so the marker survives non-simulated ticks) confines `stop_arrival_system`'s query to candidates only; (3) `CurrentLinkPolyline { link_id, points: Arc<Vec<(f32,f32)>> }` component cache refreshed by new `update_link_polyline_cache_system` (first in `MobilitySet::Advance`) — `compute_world_coord_system` and `compute_direction_system` read the cached `Arc` directly instead of doing `routes.get() → links.get() → link_polylines.get()` per entity per tick; the cache update passes `Option<&LinkId>` through the hot path (no per-entity String clone); (4) `track_chunk_populations_system` rewritten incrementally — `Query<..., Changed<Position>>` only re-buckets moved entities, `PreviousChunkByEntity` tracks each entity's old bucket, `PreviousFlowCellContrib` keeps the FlowCell aggregate slice consistent across ticks; (5) `compute_world_coord_system` writes Position with an equality guard so unchanged values don't trigger `Changed<Position>` — without this the incremental track_chunk_populations degenerated to a full rebuild because bevy's `Mut<T>::deref_mut` marks the component changed on every write regardless of value; (6) lazy chunk-activity filter inside `boarding_alighting_system` (covered in #1) — no 100k pre-pass. New profile example `examples/profile_lod_tick.rs` and `MobilityWorld::profile_world_mut()` accessor support per-system timing during development. `CurrentLinkPolyline` is derived state and not serialized — snapshot roundtrip stays byte-stable. Bench: `tick_100k_all_active` 24.4 ms → 12.77 ms (-47.7%). Per-system: boarding 6.59 → 0.01 ms; direction 3.79 → 1.91 ms; world_coord 3.73 → 1.62 ms; stop_arrive 3.60 → 0.00 ms; track_pop 2.49 → 0.43 ms. Spec absolute target < 5 ms NOT met on this host (the spec was captured with ~2× faster numbers; same machine showed ~2× variance between sessions), but the ~50% relative improvement holds and the per-system reductions match the planned algorithmic complexity drops. Spec `docs/superpowers/specs/2026-05-19-mobility-lod-perf-design.md`, plan `docs/superpowers/plans/2026-05-19-mobility-lod-perf.md`, commits 7b099b9 / 89c1b87 / 07e53b2 / 1822f86 / 34fb672 / 453a563 / a533012 / 53f6f17 / 7442e03 / 7dfdcf2 / 209d97e. All workspace cargo tests + clippy + tsc + vitest 158/158 pass; browser smoke skipped — dev stack not running locally (backend-only perf rework, no wire-protocol changes).
2026-05-19T09:57:24.000Z - Mobility no-fallback cleanup (Phase 7b follow-up): replaced the two-tier geometry resolution (hardcoded `mobility_geometry::{link,stop,route_link_world_coord}` tables as fallback chained behind the registered `LinkPolylines` resource) with a single source of truth — the `LinkPolylines` resource. `tiny_world` now `set_link_polyline`s `link:horizontal:main`, `link:vertical:main`, `link:walk:default` explicitly (chunk-centre coords). `from_network` was reordered so corridor/arterial/tram polylines are registered BEFORE any spawn, otherwise spawn-time `agent_world_coord`/`vehicle_world_coord` would miss and Positions would default to (0,0). The free helpers `agent_world_coord` + `vehicle_world_coord` drop their `resolve_link_points` + `stop_geometry` fallbacks; `MobilityWorld::resolve_link_polyline` + `world_coord_for_agent`'s stop-geometry fallback are gone. `tick_mobility`'s delta builder drops `unwrap_or((0,0))` for unresolvable geometry — entities are now skipped from the delta (no silent chunk(0,0) bucket). `MobilityWorld::Clone` now copies `ChunkActivities`, `ChunkActivityCooldowns`, and `FlowCells` so the snapshot-clone path no longer drops LOD state on every persist (previously: every snapshot wrote `flow_cells: []`, hydration resurrected an empty world). Production seed reorder + Clone fix means a fresh `DELETE FROM mobility_snapshots WHERE world_id='abutown-main'` boots a world with 1011 agents and 55 vehicles at tick 0 (was 0/0 before). Deleted: `mobility_geometry::link_geometry`/`stop_geometry`/`route_link_world_coord` + their tests (no production callers after the fallback strip). Three sim-core unit tests that previously leaned on hardcoded geometry (`world_coord_for_*` x2 + `sample_world` shared by 5 tests) were updated to register polylines explicitly. Browser smoke (`scripts/smoke-7b.mjs`): all 9 checks pass including the previously-failing `got_chunk_deltas_per_tick` + `client_receives_entity_data`. Workspace cargo + clippy + vitest 158/158 + tsc + vite build all clean. Side fix: `.env` `DATABASE_URL` updated from the IPv6-only `db.<project>.supabase.co` host to `aws-1-eu-west-1.pooler.supabase.com` since sqlx cannot reach IPv6 on this machine.
2026-05-18T13:34:47.000Z - Phase 7b WS multi-client scalability: replaced the global filtered-delta broadcast with per-chunk `tokio::broadcast` channels. `MobilityWorld::tick_mobility` now returns `HashMap<ChunkCoord, MobilityChunkDelta>`, computing `left_*` via new `PreviousAgentChunks`/`PreviousVehicleChunks` resources that track each entity's chunk at end-of-tick. New `AppState.chunk_channels: Arc<DashMap<ChunkCoord, broadcast::Sender<MobilityChunkDeltaDto>>>` holds one channel per Active|Hot chunk (created on first subscribe, reaped on last unsubscribe). Each WS handler maintains a `tokio_stream::StreamMap<ChunkCoord, BroadcastStream<…>>` for dynamic per-chunk multiplexing; `tokio::select!` reads it alongside `socket.recv` and the (now pulse-only) legacy broadcast. Subscribe emits one `ServerMessageDto::MobilityChunkSnapshot` per added chunk (replaces the Phase-7a synthetic delta); ticks emit per-chunk `MobilityChunkDelta` only for chunks with changes. The lagged-receiver recovery path re-snapshots the affected chunk. The old `ServerMessageDto::MobilityDelta` variant, `MobilityDeltaDto`, `filtered_mobility_delta_from_dto`, `synthetic_mobility_delta_for_subscription`, `next_mobility_delta`, `next_server_messages`, `build_mobility_delta_dto`, `build_filtered_mobility_delta_dto`, and the `last_visible_*` fields on `ConnectionState` are all gone — state-of-the-art wire protocol, no per-client filter step. Per-client per-tick CPU now scales with `N_subscribed_chunks`, not `N_world_entities`. Frontend learns `MobilityChunkDeltaDto` + `MobilityChunkSnapshotDto`; `applyMobilityChunkSnapshot` replaces all entities for the snapshot's chunk (purge-then-insert via `Math.floor(coord.x / 32)` chunk-of helper), `applyMobilityChunkDelta` merges `changed_*` and drops `left_*`. Browser smoke (`scripts/smoke-7b.mjs`) confirms: subscribe emits one snapshot per visible chunk, pan triggers new snapshots, no legacy `mobility_delta` frames flow, zero console errors. Workspace cargo + vitest + clippy + tsc all clean; cargo 178/178 tests, vitest 158/158. Phase 7b of the WS scalability arc; Phase 7c (Arc-snapshot lock-free reads) is the next architectural step.
2026-05-18T10:08:34.000Z - Phase 7a WS multi-client scalability: replaced `Arc<Mutex<SimulationRuntime>>` with `Arc<RwLock<SimulationRuntime>>` so per-client mobility-delta filters (`filtered_mobility_delta_from_dto`) run in parallel under `.read()` while the tick loop / snapshot loop / `apply_subscription_diff` take exclusive `.write()`. Required adding `+ Sync` to the three store traits (`ChunkSnapshotStore`, `MobilitySnapshotStore`, `WorldEventStore`) since `Arc<RwLock<T>>: Send` requires `T: Send + Sync`; all existing implementations (Postgres via `PgPool`, in-memory via plain collections) are naturally `Sync`. Frontend `chunkSubscriptionClient` is now stateful — `update(visible)` diffs against the cached subscription set (a `Map<string, ChunkCoordDto>`) and emits `chunk_subscribe(added)` / `chunk_unsubscribe(removed)` only on change. A 200 ms poll in `connectMobilityBackend` recomputes `visibleChunks(camera, viewport, world, 32, margin=1)` and feeds the diff; injectable `setIntervalImpl`/`clearIntervalImpl` match the existing test-time injection pattern. `visibleChunks` is a pure function (4 screen corners → `screenToWorld` → `chunkOf` AABB + 1-chunk ring → clamp to world bounds; `chunkOf` uses `Math.floor` to match the backend `div_euclid`). Three-client disjoint-subscription integration test exercises the new read-side parallelism. Per-client bandwidth no longer scales with world size, only with viewport size. Phase 7a of the WS scalability arc; Phase 7b (per-chunk broadcast channels + Arc-snapshot lock-free reads) is the next architectural step.
2026-05-17T21:43:58.000Z - Chunk-LOD mobility: `MobilityActivity { Hot, Active, Warm, Asleep }` per chunk driven by client-subscriber count + population with 30-tick hysteresis. Hot/Active chunks tick at full ECS fidelity; Warm chunks run a gravity-flow OD aggregate at 1 Hz via `warm_chunk_flow_system`; Asleep chunks skip the Advance/Output pipeline entirely. Promote/demote transitions spawn/despawn discrete agents in/out of `FlowCell` aggregates, preserving total population (round-trip lifecycle covered by `tests/mobility_lod_lifecycle.rs`). New resources: `ChunkActivities`, `ChunkActivityCooldowns`, `FlowCells`, `ChunkSubscribers`, `ChunkPopulations`, `ChunkTransitions`. A new `MobilitySet::LOD` runs first in the schedule before `Advance`. The WS task forwards subscribe/unsubscribe/disconnect into `ChunkSubscribers` via `MobilityWorld::update_chunk_subscribers`; `stream_world_deltas` refactored to a single cleanup block so disconnect always decrements held subscriptions. Persistence shape extended with `flow_cells` + `chunk_activities` (deterministic sorted `Vec<(ChunkCoord, _)>` tuples since `ChunkCoord` is a struct key, not a string); legacy Phase-3 snapshots stay loadable via `#[serde(default)]`. New criterion bench `mobility_tick_lod` reports **~13.6 ms/tick at 100k entities with 5 subscribed chunks** (above the informal <5 ms target — follow-up perf work likely needs to revisit per-tick chunk-activity filtering in the Advance/Output systems). Workspace `cargo test` + clippy clean; `tsc --noEmit` clean; `vitest` 158/160 pass (same 2 pre-existing `noRetiredAssets` failures); `npm run build` hits intermittent `ETIMEDOUT` while vite copies the large `public/simutrans-assets/` tree — filesystem flake, not a code regression. Phase 6 of the million-agent roadmap.
2026-05-17T00:03:53.000Z - Mobility hot-path migrated to bevy_ecs: `MobilityWorld` is now `{ world: bevy_ecs::World, schedule: Schedule, by_agent_id, by_vehicle_id }`. All entity state lives in Components (`Position`, `Direction`, `SpriteKey`, `AgentMarker`/`VehicleMarker`, `StableAgentId`/`StableVehicleId`, `AgentMobilityStateComponent`, `WalkPlan`, `WalkSpeed`, `VehicleKindComponent`, `RoutePosition`, `Capacity`, `Occupants`, `DwellTicksRemaining`). Tick logic runs as bevy systems orchestrated by a `Schedule` with `Advance → Output → Bookkeeping` SystemSets: `walk_advance` → `boarding_alighting` → `stop_arrival` → `vehicle_advance` (ordered so a single tick keeps the legacy arrive-then-board cadence) → `compute_world_coord` + `compute_direction` → `tick_increment`. Boarding/alighting uses `ParamSet` for disjoint agent+vehicle queries. Custom serde on `MobilityWorld` reconstructs the legacy `{ tick, agents, vehicles, stops, routes, link_polylines }` JSONB shape byte-for-byte (verified by `phase3-mobility-snapshot.json` round-trip integration test). 72/72 sim-core lib tests + 154/154 workspace tests + clippy clean. Criterion benchmark `mobility_tick` reports **1.44 ms/tick at 10k walkers + 1k cars** — well below the 16 ms frame budget, unlocks the path to ~100k entities. Phase 5 of the million-agent roadmap.
2026-05-27T23:02:00.000Z - Base World Cutover verification pass: production runtime now loads canonical `data/worlds/zurich-river-city-v1` instead of old demo/seeded chunks, backend `/base-world` serves bundle-derived terrain/transport/building/decor layers, and frontend renders only the backend-supplied Mini-Metro-style bundle. Base-world counts are stable at 21834 terrain tiles, 3396 roads, 256 rails, 2268 buildings, 4325 trees, and 280 details. Production fallback greps are clean across runtime entrypoints for `SEEDED_CHUNKS`, `CityNetwork::empty_for_world`, procedural Zurich runtime authority, and retired pak/simutrans/opengfx references; remaining `tiny_world` usage is test-only. Verification: `cargo fmt --manifest-path backend/Cargo.toml --all` passed, `cargo test --manifest-path backend/Cargo.toml -p sim-server` passed 48 lib + 15 HTTP + 10 WebSocket tests, `cargo test --manifest-path backend/Cargo.toml -p sim-core` passed 215 lib + 3 base-world + 2 LOD lifecycle + 8 persistence tests, `cargo clippy --manifest-path backend/Cargo.toml --workspace --all-targets -- -D warnings` is clean, `npm test` passed 41 files / 192 tests, `npm run generate:base-world` emitted the expected bundle counts, `npm run build` is clean, and `PATH=/Users/ramonfuglister/.rustup/toolchains/stable-aarch64-apple-darwin/bin:$PATH npm run test:e2e -- tests/e2e/render-smoke.spec.ts` passed. In-app browser QA on `http://127.0.0.1:5175/` showed the new minimal map with no console errors, changing screenshot hashes over 2.5s, and backend `/base-world` world id `zurich-river-city-v1`. Open follow-up: Task 8 snapshot compatibility metadata remains pending; current hydrate path rejects/reseeds vehicleless snapshots for the new base-world expected mobility, but there is not yet an explicit persisted snapshot schema/world-bundle compatibility record.
